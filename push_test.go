package main

/*
import (
	"fmt"
	//"labix.org/v2/mgo/bson"
	"libs"
	//"sync"
	"testing"
	//"time"
	"models"
)

func TestPushState(t *testing.T) {
	pusher := libs.NewPusher()
	err := pusher.SetState(1, "push.cls", models.PUSH_STATE_NOSET)

	fmt.Println(err)
}
*/
