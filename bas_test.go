package main

/*
import (
	"./libs"
	//"./models"
	"fmt"
	//"strconv"
	"testing"
)

func TestGames(t *testing.T) {
	bas := &libs.Bas{}
	gs := bas.Games()
	if len(gs) == 0 {
		t.Fatal("空")
	}
	gs = bas.Games()
	fmt.Println(gs)
}

func TestMatchs(t *testing.T) {
	bas := &libs.Bas{}
	ms := bas.Matchs()
	if len(ms) == 0 {
		t.Fatal("空")
	}
	ms = bas.Matchs()
	fmt.Println(ms)
}
*/
